{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and capture overlays. IMPORTANT: a window that calls any Tauri API (invoke / event listen) must be listed in `windows` below, otherwise every call is silently denied. Windows that only paint (recframe-*) are deliberately left out to keep least privilege.",
  "windows": ["main", "overlay-*", "editor", "pin-*", "video-editor", "reccountdown"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-set-size",
    "core:window:allow-center",
    "core:window:allow-outer-size",
    "core:window:allow-scale-factor",
    "core:window:allow-start-dragging",
    "opener:default",
    "global-shortcut:allow-is-registered",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister"
  ]
}
