# AuraCap

AI搭載を見据えたWindows向け画面キャプチャツール。まず開発者自身が毎日使う軽量ツールとして作り、価値が実証できたら製品化する。
A screen capture tool for Windows with future AI capabilities. Built first as a daily-driver tool for its own developer, to be productized once its value is proven.

## Language

### キャプチャ / Capture

**キャプチャ / Capture**:
画面の一部または全体を静止画として取得する行為。v0.1は静止画のみ（動画は対象外）。
_Avoid_: スクショ（UI文言では使わない）, 録画

**キャプチャモード / Capture Mode**:
何を撮るかの選択。「領域選択」「ウィンドウ単位」「全画面（モニター単位）」の3種。
_Avoid_: 撮影タイプ

**フリーズフレーム / Freeze Frame**:
ホットキー押下の瞬間に先に全画面を取得し、その静止画上で領域を選択させる方式。自己写り込みが原理的に発生しない。
_Avoid_: ライブ選択, スナップショットオーバーレイ

**領域選択 / Region Selection**:
フリーズフレーム上をドラッグして矩形範囲を切り出す操作。UI上の名称は「矩形キャプチャ」。
_Avoid_: 範囲指定, クロップ（編集時のトリミングと区別する）

**ウィンドウピッカー / Window Picker**:
フリーズフレーム上でホバー中のウィンドウを枠でハイライトし、クリックで選択させるUI。Z順で最前面のウィンドウが当たる。
_Avoid_: ウィンドウ選択モード（操作全体はキャプチャモードの「ウィンドウ単位」を使う）

**調整モード / Adjust Phase**:
ウィンドウ選択の直後に入る確定前の状態。8方向ハンドルでリサイズ、内側ドラッグで移動でき、Enter/✓で確定する。矩形キャプチャはドラッグを離した瞬間に即確定するため調整モードに入らない。
_Avoid_: 編集モード（軽量エディタと区別する）

**ルーペ / Loupe**:
矩形選択中・調整モードの枠変更中にカーソル付近へ表示される拡大鏡。周辺ピクセルを8倍表示し、十字線と座標・サイズ表示で1px単位の位置合わせを支援する。デフォルト位置はカーソル左下。
_Avoid_: 拡大鏡（UI文言・コードともルーペで統一）, マグニファイア

**常駐オーバーレイ / Resident Overlay**:
起動時にモニター毎に事前生成され、非表示のまま使い回されるオーバーレイウィンドウ。キャプチャ切り替えを高速化する。
_Avoid_: 使い捨てオーバーレイ

**フルページキャプチャ / Full-Page Capture**:
ブラウザ拡張がページ全体（スクロール領域込み）を一発で取得し、ブリッジ経由でAuraCapへ送るキャプチャ。対象はブラウザのみ（ADR 0003）。
_Avoid_: スクロールキャプチャ（スクロール合成はしないため）

**ブリッジ / Bridge**:
ブラウザ拡張からPNGを受け取るローカルHTTPサーバー（127.0.0.1:14820）。受信画像は通常キャプチャと同じ後処理（履歴→クリップボード→エディタ）へ流れる。
_Avoid_: API サーバー

### 編集・保存 / Editing & Saving

**軽量エディタ / Quick Editor**:
キャプチャ直後に即起動する小さな編集ウィンドウ。アノテーションと出力操作のみを担う。Snagit級のフルエディタは目指さない。
_Avoid_: フルエディタ, ビューア

**アノテーション / Annotation**:
画像への書き込み要素。v0.1は矩形枠・矢印・テキスト・モザイク・トリミング・番号バッジ・ハイライトの7種。
_Avoid_: マークアップ, 装飾

**番号バッジ / Number Badge**:
手順説明用の連番スタンプ（①②③）。将来のSOP機能の布石。
_Avoid_: ステップマーカー

**履歴 / History**:
キャプチャした瞬間に無条件で自動保存されるPNGの保管場所。上限枚数で自動ローテーションする。
_Avoid_: ライブラリ, アーカイブ

**出力 / Export**:
編集後の画像をユーザーの明示操作でクリップボードまたはファイルへ書き出すこと。履歴への自動保存とは区別する。
_Avoid_: 保存（履歴の自動保存と曖昧になるため単独では使わない）

### AI機能 / AI Features

**Smart Redact**:
Windows標準OCRと正規表現で機密情報（メール・電話・カード番号・APIキー・シークレット・IP）の領域を検出し、ぼかしとして自動追加する機能。完全ローカル処理。UI名は「自動マスク」。
_Avoid_: 自動モザイク（ぼかしで統一）

**Screenshot-to-Code**:
編集後のスクリーンショットをClaude APIへ送り、Tailwind CSSの単一HTMLとして再現コードを生成する機能。APIキーは設定に保存。UI名は「コード生成」。
_Avoid_: UI複製, デザイン変換
